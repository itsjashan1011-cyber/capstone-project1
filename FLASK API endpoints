
from flask import Flask, request, jsonify

app = Flask(__name__)

@app.route('/upload', methods=['POST'])
def upload_traffic_data():
    data = request.get_json()
    print("Received data from:", data['source'])
    return jsonify({"status": "success"}), 200
